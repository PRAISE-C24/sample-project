pipeline {
    agent any

    tools {
      maven 'Maven 3.9.9'
    }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello, You are welcome!!' 
                sh 'mvn --version'
            }
        }  
     }
}
